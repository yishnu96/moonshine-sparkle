# Mixpanel project token — find it in Mixpanel → Settings → Project Settings.
# Copy this file to `.env` (or set the var in Vercel Project Settings) and fill in the value.
# If left empty, Mixpanel silently no-ops and only GA4/GTM tracking runs.
VITE_MIXPANEL_TOKEN=
